{
  "name": "preview-tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @preview-tool/cli build",
    "test": "pnpm -w run build && node packages/cli/dist/index.js generate --cwd packages/cli/test-fixtures/sample-app",
    "release": "pnpm build && pnpm -r publish --access public"
  }
}
